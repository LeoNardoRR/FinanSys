{
  "name": "finansys-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --outfile=app.js",
    "check": "npm run build && node --check app.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.3"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
